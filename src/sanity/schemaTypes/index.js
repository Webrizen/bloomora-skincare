import {blockContentType} from './blockContentType';
import {categoryType} from './categoryType';
import { productsType } from './productsType';

export const schema = {
  types: [blockContentType, categoryType, productsType, ],
}
